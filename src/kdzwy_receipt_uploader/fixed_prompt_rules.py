"""Non-customizable accounting rules shared by every template company."""

FIXED_LLM_RULES = """# 系统固定规则（生成器维护）

1. `dc` 只允许 `1` 和 `-1`：`1` 是借方，`-1` 是贷方。
2. 每张凭证借方金额合计必须等于贷方金额合计。
3. 科目必须来自本次运行时动态获取的科目目录，并且必须是可记账明细科目；不得臆造科目编码、名称或 ID。
4. 科目启用辅助核算时，必须从对应 ItemClass 的动态 Item 目录选择真实对象；不得臆造辅助核算对象。
5. 分录数量由匹配到的模板决定，不得擅自增删；无法满足模板时必须返回 `blocked`。
6. 非银行凭证的所有分录使用相同摘要，摘要由模板的 `explanation_header`、一个半角空格和业务编号组成。银行凭证使用公司专属银行模板的固定业务摘要；交易对方通过辅助核算表达，不得擅自写入摘要；只有银行存款分录在摘要末尾追加一个半角空格和 OCR 交易日期，其他分录不追加日期。
7. 无法确定模板、科目、辅助核算、金额或借贷方向时必须返回 `blocked`，不得猜测。
8. 只返回系统规定的 JSON，不得返回 Markdown 或额外说明。
"""
